server:
  port: 9990
  servlet:
    context-path: '/v3'

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bladex_boot?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: 123456

mybatis:
  #type-aliases-package: com.example.entity
  mapperLocations: classpath:mappers/*.xml

logging:
  file: D:\logs\task_manager.log
  level:
    root: info
    com.hlife.task_manager.dao: debug
